// Formato de uma categoria de gasto criada pelo usuario.
// A cor fica salva junto para a categoria manter sempre a mesma identidade visual.
export interface ExpenseCategory {
  id: string;
  name: string;
  color: string;
}
